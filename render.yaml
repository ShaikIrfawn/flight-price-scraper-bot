services:
  - type: web
    name: flight-price-scraper
    runtime: node
    buildCommand: |
      apt-get update && \
      apt-get install -y chromium && \
      npm install
    startCommand: node flight-scraper.js
    envVars:
      - key: CHROME_PATH
        value: /usr/bin/chromium
